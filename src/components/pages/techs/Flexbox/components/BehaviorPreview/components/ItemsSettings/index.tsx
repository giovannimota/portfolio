// External Libraries
import React from 'react'

// Components
import { Typography } from '@components/toolkit/Typography'

// Styles
import { Container } from './styles'

interface Props {
  // Props
}

export const ItemsSettings: React.FC<Props> = (
  {
    /* Props */
  }
) => {
  return (
    <Container>
      <Typography variant="b2">Items Settings</Typography>
    </Container>
  )
}
